import * as enSettings from './en/settings'
import * as esSettings from './es/settings'
import { LanguageObj } from '@/utils/types/common'

const SETTINGS: LanguageObj = {
  en: enSettings,
  es: esSettings,
}

export default SETTINGS
